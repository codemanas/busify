<?php
/**
 * @author Deepen.
 * @created_on 12/3/19
 *
 * @since 1.0.0
 * @copyright 2019. CodeManas
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * AFter head tag hook
 */
function busify_after_head() {
	do_action( 'busify_after_head' );
}

/**
 * Main body top
 */
function busify_body_top() {
	do_action( 'busify_body_top' );
}

/**
 * Before main header hook
 */
function busify_before_header() {
	do_action( 'busify_before_header' );
}

/**
 * Main header hook
 */
function busify_header() {
	do_action( 'busify_header' );
}

/**
 * After main header hook
 */
function busify_after_header() {
	do_action( 'busify_after_header' );
}

/**
 * Main Header Content
 */
function busify_masthead_content() {
	do_action( 'busify_masthead_content' );
}

/**
 * Before Main Header Content
 */
function busify_before_masthead_content() {
	do_action( 'busify_before_masthead_content' );
}

/**
 * After Main Header Content
 */
function busify_after_masthead_content() {
	do_action( 'busify_after_masthead_content' );
}

/**
 * Before top banner image content
 */
function busify_before_top_banner_image() {
	do_action( 'busify_before_top_banner_image' );
}

/**
 * Top banner image content
 */
function busify_top_banner_image() {
	do_action( 'busify_top_banner_image' );
}

/**
 * After Top banner image content
 */
function busify_after_top_banner_image() {
	do_action( 'busify_after_top_banner_image' );
}

/**
 * Before main footer render
 */
function busify_before_footer() {
	do_action( 'busify_before_footer' );
}

/**
 * Main Footer Hook
 */
function busify_footer() {
	do_action( 'busify_footer' );
}

/**
 * After main footer render
 */
function busify_after_footer() {
	do_action( 'busify_after_footer' );
}

/**
 * Bottom footer
 */
function busify_body_bottom() {
	do_action( 'busify_body_bottom' );
}

/**
 * Output main footer html
 */
function busify_main_footer() {
	do_action( 'busify_main_footer' );
}

/**
 * Output before main footer html
 */
function busify_before_main_footer() {
	do_action( 'busify_before_main_footer' );
}

/**
 * Output after main footer html
 */
function busify_after_main_footer() {
	do_action( 'busify_after_main_footer' );
}

/**
 * Output before main super footer html
 */
function busify_before_super_footer() {
	do_action( 'busify_before_super_footer' );
}

/**
 * Super footer render
 */
function busify_super_footer() {
	do_action( 'busify_super_footer' );
}

/**
 * Output after main super footer html
 */
function busify_after_super_footer() {
	do_action( 'busify_after_super_footer' );
}

/**
 * Post Hooks Start
 */
function busify_before_main_content() {
	do_action( 'busify_before_main_content' );
}

function busify_after_main_content() {
	do_action( 'busify_after_main_content' );
}

function busify_before_post_content() {
	do_action( 'busify_before_post_content' );
}

function busify_after_post_content() {
	do_action( 'busify_after_post_content' );
}

function busify_before_sidebar() {
	do_action( 'busify_before_sidebar' );
}

function busify_after_sidebar() {
	do_action( 'busify_after_sidebar' );
}

function busify_main_content() {
	do_action( 'busify_main_content' );
}

function busify_main_comments() {
	do_action( 'busify_main_comments' );
}
/**
 * Post Hooks End
 */